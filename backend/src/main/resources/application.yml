server:
  port: ${SERVER_PORT:8080}

api:
  consulta:
    base-url: ${API1_URL}
    username: ${API1_USERNAME}
    password: ${API1_PASSWORD}

spring:
  cloud:
    openfeign:
      okhttp:
        enabled: false
      httpclient:
        enabled: true
